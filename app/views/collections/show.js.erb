if (window.history.pushState!=null) window.history.pushState(null, '', '/collections/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'collections/show')).html_safe %>");


