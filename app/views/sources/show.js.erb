if (window.history.pushState!=null) window.history.pushState(null, '', '/sources/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'sources/show')).html_safe %>");


