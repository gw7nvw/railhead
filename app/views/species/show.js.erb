if (window.history.pushState!=null) window.history.pushState(null, '', '/species/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'species/show')).html_safe %>");


