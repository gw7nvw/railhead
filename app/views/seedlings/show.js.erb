if (window.history.pushState!=null) window.history.pushState(null, '', '/seedlings/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'seedlings/show')).html_safe %>");


