if (window.history.pushState!=null) window.history.pushState(null, '', '/plantings/<%=params[:id]%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'plantings/show')).html_safe %>");


